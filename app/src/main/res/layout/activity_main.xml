<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.zhan.carl.italker.MainActivity">

    <android.support.design.widget.AppBarLayout
        android:id="@+id/appbar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/actionBarWithStatusBarSize">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="@dimen/statusBarSize"
            android:paddingLeft="@dimen/len4"
            android:paddingRight="@dimen/len4">

            <com.zhan.carl.common.widget.ProtraitView
                android:id="@+id/im_protraitView"
                android:layout_width="@dimen/len40"
                android:layout_height="@dimen/len40"
                android:layout_margin="@dimen/len4"
                android:foreground="@drawable/ic_touch_menu"
                android:padding="@dimen/len4"
                android:scaleType="centerCrop"
                android:src="@drawable/default_portrait" />

            <TextView
                android:id="@+id/txt_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:gravity="center"
                android:text="@string/app_name"
                android:textAppearance="@style/TextAppearance.Title"
                android:textColor="@color/white" />

            <ImageView
                android:id="@+id/im_search"
                android:layout_width="@dimen/len40"
                android:layout_height="@dimen/len40"
                android:layout_gravity="end|center_vertical"
                android:layout_margin="@dimen/len40"
                android:clickable="true"
                android:contentDescription="@string/app_name"
                android:foreground="@drawable/ic_touch_menu"
                android:padding="@dimen/len10"
                android:scaleType="centerInside"
                android:src="@drawable/ic_search"
                android:tint="?attr/toolbarMenuIconColor" />
        </FrameLayout>
    </android.support.design.widget.AppBarLayout>

    <FrameLayout
        android:id="@+id/layout_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="@dimen/len52"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <net.qiujuer.genius.ui.widget.FloatActionButton
        android:id="@+id/btn_action"
        android:layout_width="@dimen/len52"
        android:layout_height="@dimen/len52"
        android:layout_marginBottom="@dimen/len68"
        android:layout_marginEnd="@dimen/len16"
        android:padding="@dimen/len10"
        android:src="@drawable/ic_group_add"
        android:tint="@color/white"
        android:translationY="@dimen/len_76"
        app:gBackgroundColor="@color/colorAccent"
        app:layout_anchor="@id/layout_container"
        app:layout_anchorGravity="bottom|end" />

    <android.support.design.widget.BottomNavigationView
        android:id="@+id/navigation"
        android:layout_width="match_parent"
        android:layout_height="@dimen/len_52"
        android:layout_gravity="bottom"
        android:background="@color/g_default_accent"
        android:elevation="@dimen/len_4"
        android:outlineProvider="bounds"
        android:translationZ="@dimen/len_8"
        app:itemIconTint="@color/text_nav"
        app:itemTextColor="@color/text_nav"
        app:menu="@menu/navigation_items"
        tools:targetApi="lollipop"
        tools:visibility="invisible" />
</android.support.design.widget.CoordinatorLayout>
